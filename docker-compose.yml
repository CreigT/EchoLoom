services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      APP_ENV: development
      LLM_PROVIDER: mock
      AUDIO_PROVIDER: mock
    volumes:
      - ./data:/app/data
